<%= form_for @drink do |f| %>
  <%= f.label :name, "Drink Name: " %>
  <%= f.text_field :name %>

  <br />

  <%= f.label :user_id, "User" %>
  <%= f.collection_select :user_id, @users, :id, :name %>

  <%= f.submit %>
<% end %>
